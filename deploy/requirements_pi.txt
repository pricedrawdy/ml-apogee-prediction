# Raspberry Pi — Minimal dependency set for the flight demo
# ============================================================
# Versions are lower-bounds; use whatever is available for your
# Pi OS + Python version.  All packages are pure-Python or have
# pre-built ARM64 wheels in PyPI.

# ── Core science stack ────────────────────────────────────────
numpy>=1.24
scipy>=1.10          # used by download_sounds.py pink noise (optional)

# ── ML models ─────────────────────────────────────────────────
scikit-learn>=1.3    # RandomForest, LinearRegression, StandardScaler
joblib>=1.3          # model serialisation (.pkl)

# ── PyTorch (CPU) ─────────────────────────────────────────────
# For Pi 4/5 (aarch64), install the official ARM wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# If that is unavailable for your OS version, comment out and use
# scikit-learn models only (--model rf  or  --model lr).
torch>=2.0

# ── Audio ─────────────────────────────────────────────────────
pygame>=2.5          # pygame.mixer for WAV playback (omit if --no-audio)

# ── Serial (Arduino) ──────────────────────────────────────────
pyserial>=3.5        # only needed with --serial flag

# ── Utilities ─────────────────────────────────────────────────
pandas>=2.0          # only used by inference_engine if you load CSV
